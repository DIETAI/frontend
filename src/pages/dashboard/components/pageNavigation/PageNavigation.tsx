import React from "react";

import * as Styled from "./PageNavigation.styles";

const PageNavigation = () => {
  return (
    <Styled.PageNavigationWrapper>
      <a>produkty / nowy</a>
    </Styled.PageNavigationWrapper>
  );
};

export default PageNavigation;
